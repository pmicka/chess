# .gitignore
# Intent: Keep secrets and runtime data out of GitHub.
# - config.php contains secret keys and environment settings
# - data/ contains runtime state (SQLite) and must never be committed

# OS / editor noise
.DS_Store
Thumbs.db
.vscode/
.idea/

# Secrets (server-only)
config.local.php

# Runtime data (server-only)
data/*
!data/.gitkeep
!data/.htaccess

# Logs
*.log
